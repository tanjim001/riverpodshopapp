    import 'package:elearnapp/constant/iconstring.dart';
import 'package:flutter/material.dart';

List<ImageIcon> navigationIcons = [
      const ImageIcon(
        AssetImage(home),
        size: 20,
      ),
      const ImageIcon(AssetImage(search), size: 20),
      const ImageIcon(
        AssetImage(playcircle),
        size: 20,
      ),
      const ImageIcon(
        AssetImage(messagecircle),
        size: 20,
      ),
      const ImageIcon(
        AssetImage(person2),
        size: 20,
      ),
    ];